# HTTPS 证书配置指南

为了让 Now Playing 能够获取到网易云音乐的歌词和进度，必须拦截其 HTTPS 流量。请按照以下步骤安装证书。

## 1. 启动服务
确保您已经在终端中运行了 `node index.js`，并且没有报错。

## 2. 下载证书
打开浏览器，访问：
[http://localhost:8002/fetchCrtFile](http://localhost:8002/fetchCrtFile)

浏览器会下载一个名为 `rootCA.crt` 的文件。

## 3. 安装证书 (关键步骤)
1.  **双击** 下载好的 `rootCA.crt` 文件。
2.  点击 **“安装证书” (Install Certificate)** 按钮。
3.  在存储位置选择中，选择 **“本地计算机” (Local Machine)**（需要管理员权限），然后点击“下一步”。
4.  选择 **“将所有的证书都放入下列存储” (Place all certificates in the following store)**。
5.  点击 **“浏览” (Browse)**，选择 **“受信任的根证书颁发机构” (Trusted Root Certification Authorities)**。
    > **注意**：必须选这个，否则无效！
6.  点击“确定”，然后“下一步”，最后“完成”。
7.  系统会提示导入成功。

## 4. 重启网易云音乐
1.  **彻底关闭** 网易云音乐客户端（右键任务栏托盘图标 -> 退出）。
2.  重新打开网易云音乐。
3.  如果配置正确，您现在应该能正常听歌，且在 `http://localhost:8003` 看到歌词。

## 5. 故障排除
*   **如果网易云提示“网络不通”**：
    *   检查证书是否安装到了“受信任的根证书颁发机构”。
    *   检查网易云代理设置是否为 `127.0.0.1:8001`。
*   **如果想卸载证书**：
    *   按 `Win+R`，输入 `certmgr.msc`。
    *   展开 “受信任的根证书颁发机构” -> “证书”。
    *   找到 `AnyProxy` 相关的证书并删除。
